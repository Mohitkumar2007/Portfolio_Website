import { BarChart3,Briefcase,Code2,FileText,LogOut,Mail,UserRound } from 'lucide-react'; import { NavLink,Outlet,useNavigate } from 'react-router-dom'; import { supabase } from '@shared/supabase'
const nav=[['/dashboard','Dashboard',BarChart3],['/projects','Projects',Briefcase],['/skills','Skills',Code2],['/about','About',UserRound],['/contact','Contact',Mail]] as const
export function AdminLayout(){const go=useNavigate();async function logout(){await supabase.auth.signOut();go('/login')};return <div className="min-h-screen bg-slate-50 md:flex"><aside className="flex w-full flex-col bg-slate-800 text-slate-200 md:fixed md:inset-y-0 md:w-60"><div className="px-6 py-7 text-xl font-bold text-white">CMS Admin</div><nav className="flex gap-1 overflow-auto px-3 md:block">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm ${isActive?'bg-slate-700 text-white':'text-slate-300 hover:bg-slate-700'}`}><Icon size={17}/>{label}</NavLink>)}</nav><div className="mt-auto px-4 py-5"><a className="mb-3 block text-sm text-sky-300" href="http://localhost:5173" target="_blank">View Portfolio →</a><button onClick={logout} className="flex items-center gap-2 text-sm text-slate-300"><LogOut size={16}/>Logout</button></div></aside><section className="min-h-screen flex-1 p-6 md:ml-60 md:p-10"><Outlet/></section></div>}
