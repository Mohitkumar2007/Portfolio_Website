import { ArrowDownRight, Download } from 'lucide-react'; import { motion, useReducedMotion } from 'framer-motion'; import { useContact } from '@shared/hooks/useContact'
export function Hero(){const {contact}=useContact();const reduceMotion=useReducedMotion();const item={hidden:{opacity:0,y:reduceMotion?0:18},show:{opacity:1,y:0}};return <section id="top" className="hero hero-upgraded"><motion.div className="wrap" initial="hidden" animate="show" variants={{show:{transition:{staggerChildren:reduceMotion?0:.1}}}}><motion.p variants={item} className="eyebrow">B.E. Data Science · Bengaluru</motion.p><motion.div variants={item} className="status-pill">Open to Internships · AI / Data Science / Full Stack</motion.div><motion.h1 variants={item}>Building AI Products <em>with Data,</em><br/> Design & Code</motion.h1><motion.p variants={item} className="hero-copy">I’m Mohit Kumar A, a Data Science student and AI developer building practical machine learning, full-stack, and analytics projects.</motion.p><motion.div variants={item} className="hero-actions"><a className="button" href="#work">View Projects <ArrowDownRight size={18}/></a><a className="button ghost" href={contact?.resume_url ?? '#contact'} target={contact?.resume_url ? '_blank' : undefined} rel={contact?.resume_url ? 'noopener noreferrer' : undefined}><Download size={16}/>Download Resume</a></motion.div></motion.div></section>}
