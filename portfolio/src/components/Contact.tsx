import { ArrowUpRight } from 'lucide-react'; import { useContact } from '@shared/hooks/useContact'
export function Contact(){const {contact}=useContact();return <section id="contact" className="section"><div className="wrap contact"><p className="eyebrow">Have an idea?</p><h2 className="title">Let’s make it real.</h2><a className="contact-mail" href={`mailto:${contact?.email??''}`}>{contact?.email??'hello@mohit.dev'} <ArrowUpRight/></a><div className="socials">{contact?.linkedin_url&&<a href={contact.linkedin_url} target="_blank" rel="noopener noreferrer">LinkedIn</a>}{contact?.github_url&&<a href={contact.github_url} target="_blank" rel="noopener noreferrer">GitHub</a>}{contact?.resume_url&&<a href={contact.resume_url} target="_blank" rel="noopener noreferrer">Résumé</a>}</div></div></section>}
