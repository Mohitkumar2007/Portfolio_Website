import type { TimelineEntry } from '@shared/types'
import { useAbout } from '@shared/hooks/useAbout'
import { Reveal } from './Reveal'
export function AboutTimeline(){const {about}=useAbout();let timeline:TimelineEntry[]=[];try{timeline=typeof about?.timeline==='string'?JSON.parse(about.timeline):about?.timeline??[]}catch{}return <section id="about" className="section"><div className="wrap about-upgraded"><Reveal><div><p className="eyebrow">About me</p><h2 className="title">Curious by default.</h2><p className="about-intro">I enjoy building practical AI and software projects that solve real problems. My focus is on machine learning, full-stack development, databases, and clean user interfaces.</p><p className="muted bio">{about?.bio}</p></div></Reveal><Reveal delay={.12}><div className="timeline-card"><p className="eyebrow">Career path</p>{timeline.map(entry=><article className="timeline-item" key={`${entry.year}-${entry.title}`}><span className="timeline-line"/><div><b>{entry.year}</b><h3>{entry.title}</h3><p>{entry.description}</p></div></article>)}</div></Reveal></div></section>}
