import { useAbout } from '@shared/hooks/useAbout'; import type { TimelineEntry } from '@shared/types'
export function About(){const {about}=useAbout();let timeline:TimelineEntry[]=[];try{timeline=typeof about?.timeline==='string'?JSON.parse(about.timeline):about?.timeline??[]}catch{}return <section id="about" className="section"><div className="wrap about"><div><p className="eyebrow">About me</p><h2 className="title">Curious by default.</h2></div><div><p className="muted bio">{about?.bio??'I care about the details that make software feel considered, from the first idea to the last interaction.'}</p><div className="timeline">{timeline.map(t=><div key={t.year+t.title}><b>{t.year}</b><span><strong>{t.title}</strong><small>{t.description}</small></span></div>)}</div></div></div></section>}
