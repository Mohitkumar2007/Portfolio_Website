import { ExternalLink, Github } from 'lucide-react'; import { useProjects } from '@shared/hooks/useProjects'; import type { Project } from '@shared/types'
export function Projects(){const {projects,loading}=useProjects();return <section id="work" className="section"><div className="wrap"><p className="eyebrow">Selected work</p><h2 className="title">Built to be used.</h2><div className="projects">{loading&&<p className="muted">Loading work…</p>}{projects.map((p:Project,i:number)=><article className="project card" key={p.id??p.title}><div className="project-num">0{i+1}</div>{p.image_url&&<img src={p.image_url} alt=""/>}<div className="project-body"><h3>{p.title}</h3><p className="muted">{p.description}</p><div className="tags">{p.stack?.map((s:string)=><span className="tag" key={s}>{s}</span>)}</div><div className="project-links">{p.github_url&&<a href={p.github_url} target="_blank" rel="noopener noreferrer"><Github size={17}/> Code</a>}{p.demo_url&&<a href={p.demo_url} target="_blank" rel="noopener noreferrer"><ExternalLink size={17}/> Visit</a>}</div></div></article>)}</div></div></section>}
