import { useSkills } from '@shared/hooks/useSkills'; import type { Skill } from '@shared/types'
export function Skills(){const {skills}=useSkills();const groups=Object.entries(skills.reduce<Record<string,string[]>>((a:Record<string,string[]>,s:Skill)=>{(a[s.category]??=[]).push(s.name);return a},{}));return <section id="skills" className="section skills"><div className="wrap"><p className="eyebrow">Toolkit</p><h2 className="title">A broad, focused stack.</h2><div className="skill-grid">{groups.map(([cat,names])=><div key={cat}><h3>{cat}</h3>{names.map((n:string)=><span className="tag" key={n}>{n}</span>)}</div>)}</div></div></section>}
